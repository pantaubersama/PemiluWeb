// initial state
const state = {
  snack: ''
}

// actions
const actions = {
  
}

// mutations
const mutations = {
  setSnack (state, snack) {
    state.snack = snack
  }
}

// getters
const getters = {
  
}


export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}